@layer components {
  .gradient-text {
    /* Set `text-tx`, `from-*` and `to-*` classes separately. */
    @apply bg-clip-text bg-gradient-to-r box-decoration-clone;
    @apply transition-colors duration-200;
  }
}
